le(!q.empty()) {
    //     int curr = q.front();q.pop();
    //     for (int i=0; i<rooms[curr].size(); ++i) {
    //         if (rooms[curr][i] != -1) {
    //             q.push(rooms[curr][i]);
    //             rooms[curr][i] = -1;
    //             vis[rooms[curr][i]] = true;
    //         }
    //     }
    // }